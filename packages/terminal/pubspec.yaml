name: terminal
description: Terminal rendering with libghostty and PTY support
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  ffi: ^2.1.0
  flutter:
    sdk: flutter
  core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  ffigen: ^9.0.0
  mocktail: ^1.0.0
