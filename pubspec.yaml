name: agent_terminal
description: AI Agent Orchestration Terminal
version: 1.0.0
license: MIT

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.0
  path_provider: ^2.1.0
  core:
    path: packages/core
  ui:
    path: packages/ui
  terminal:
    path: packages/terminal

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0

flutter:
  uses-material-design: true
